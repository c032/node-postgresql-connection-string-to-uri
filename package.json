{
  "name": "node-postgresql-connection-string-to-uri",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "fmt": "fmt",
    "lint": "lint",
    "test": "npm run build && cd ./build/ && node --test ."
  },
  "devDependencies": {
    "@types/node": "^20.11.16",
    "node-base-typescript": "github:c032/node-base-typescript"
  }
}
